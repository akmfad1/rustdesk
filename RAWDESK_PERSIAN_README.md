# راهنمای ساخت RAWdesk

## خلاصه

این مخزن برای ساخت **RAWdesk** آماده شده است - یک نسخه سفارشی از RustDesk با تنظیمات از پیش تعیین شده.

## تنظیمات انجام شده ✅

| تنظیمات | مقدار |
|---------|-------|
| نام برنامه | RAWdesk |
| آدرس سرور | desk.timextools.ir |
| کلید عمومی | vuw04gfylmMzuWj2nFEV5a2gMbfsdnKwvbwsqhGBu0o= |
| رمز عبور دائمی | @dmin110 |
| نوع اتصال | فقط ورودی (Incoming Only) |
| پلتفرم | ویندوز 32 بیتی |
| نوع نصب | قابل حمل (بدون نصب) |
| سطح دسترسی | کامل |

## روش ساخت (آسان)

### گام 1: اجرای Workflow در GitHub

1. به تب **Actions** در GitHub بروید
2. روی **Build RAWdesk (Windows 32-bit)** کلیک کنید
3. دکمه **Run workflow** را بزنید
4. منتظر بمانید تا ساخت کامل شود (15-20 دقیقه)

### گام 2: دانلود فایل ساخته شده

1. روی workflow تکمیل شده کلیک کنید
2. فایل artifact با نام `RAWdesk-windows-x86-portable` را دانلود کنید
3. فایل ZIP را Extract کنید

### گام 3: استفاده

فایل‌های موجود در ZIP:
- `RAWdesk.exe` - فایل اصلی برنامه
- `sciter.dll` - کتابخانه رابط کاربری
- `README.txt` - اطلاعات تنظیمات

**نحوه استفاده**:
1. هر دو فایل را در یک پوشه قرار دهید
2. `RAWdesk.exe` را اجرا کنید
3. نصب لازم نیست - مستقیم اجرا می‌شود

## ویژگی‌های خاص

### 🔒 اتصال فقط ورودی (Incoming Only)

RAWdesk فقط می‌تواند اتصالات ورودی را دریافت کند:
- ✅ دیگران می‌توانند به این کامپیوتر متصل شوند
- ✅ می‌توان از راه دور کنترل شود
- ❌ نمی‌تواند به کامپیوترهای دیگر متصل شود
- ❌ امکان کنترل سایر کامپیوترها وجود ندارد

### 🔐 تنظیمات قفل شده

کاربران نمی‌توانند این موارد را تغییر دهند:
- ❌ آدرس سرور
- ❌ رمز عبور دائمی
- ❌ تنظیمات اتصال
- ❌ تنظیمات امنیتی

این امر استقرار یکپارچه در تمام کلاینت‌ها را تضمین می‌کند.

### 🎨 آیکون و لوگوی سفارشی

می‌توانید آیکون و لوگوی خود را اضافه کنید:

1. **فایل‌های مورد نیاز ایجاد کنید**:
   - `icon.ico` - آیکون اصلی ویندوز
   - `tray-icon.ico` - آیکون سیستم تری
   - `icon.png` - آیکون عمومی
   - `logo.svg` - لوگوی اصلی
   - `banner.svg` - بنر/هدر

2. **در پوشه `res/rawdesk/` قرار دهید**

3. **اسکریپت را اجرا کنید**:
   ```bash
   ./customize-icons.sh
   ```

4. **دوباره workflow را اجرا کنید**

راهنمای کامل در فایل `res/rawdesk/README.md` موجود است.

## فایل‌های مستندات

این مخزن شامل راهنماهای کامل است:

- **RAWDESK_QUICKSTART.md** - راهنمای سریع (انگلیسی)
- **RAWDESK_BUILD_GUIDE.md** - راهنمای جامع ساخت (انگلیسی)
- **RAWDESK_IMPLEMENTATION_SUMMARY.md** - خلاصه پیاده‌سازی (انگلیسی)
- **res/rawdesk/README.md** - راهنمای سفارشی‌سازی آیکون (انگلیسی)
- **RAWDESK_PERSIAN_README.md** - این فایل (فارسی)

## نکات امنیتی ⚠️

1. **رمز عبور در کد**: رمز عبور دائمی (`@dmin110`) در کد قابل مشاهده است
   - برای استقرارهای کنترل شده مناسب است
   - برای توزیع عمومی توصیه نمی‌شود

2. **امنیت کلید خصوصی**: کلید خصوصی (متناظر با کلید عمومی) را ایمن نگه دارید
   - کلید خصوصی در این مخزن نیست
   - هر کسی که کلید خصوصی را داشته باشد می‌تواند ارتباطات را رمزگشایی کند

3. **امنیت سرور**: اطمینان حاصل کنید `desk.timextools.ir` به درستی ایمن شده است
   - سرور باید کلید خصوصی متناظر را داشته باشد
   - نرم‌افزار سرور را به‌روز نگه دارید

4. **دسترسی کامل**: این build دارای مجوزهای دسترسی کامل است
   - کنترل کنید چه کسانی می‌توانند متصل شوند

## عیب‌یابی

### مشکلات ساخت

**مشکل**: Workflow با خطا مواجه می‌شود
- **راه حل**: workflow را دوباره اجرا کنید

**مشکل**: خطای YAML syntax
- **راه حل**: فایل workflow را بررسی کنید

### مشکلات اجرا

**مشکل**: اتصال به سرور برقرار نمی‌شود
- بررسی کنید: سرور در حال اجرا و قابل دسترسی است
- بررسی کنید: فایروال اتصالات را مجاز می‌کند
- بررسی کنید: کلید عمومی با تنظیمات سرور مطابقت دارد

**مشکل**: آیکون‌ها ظاهر نمی‌شوند
- بررسی کنید: آیکون‌ها قبل از build در `res/rawdesk/` قرار گرفته‌اند
- بررسی کنید: اسکریپت `customize-icons.sh` اجرا شده است
- بررسی کنید: workflow بعد از سفارشی‌سازی آیکون اجرا شده است

## پیش‌نیازهای سرور

برای استفاده از RAWdesk، نیاز دارید:

1. **سرور RustDesk** در حال اجرا باشد:
   - آدرس: `desk.timextools.ir`
   - با کلید خصوصی متناظر با کلید عمومی

2. **پورت‌های باز** (معمولاً 21115-21119)

3. **فایروال تنظیم شده** برای اجازه اتصالات RustDesk

## مراحل استقرار

### برای مدیر

- [ ] سرور `desk.timextools.ir` در حال اجرای rustdesk-server است
- [ ] سرور کلید خصوصی متناظر دارد
- [ ] سرور از شبکه‌های کلاینت قابل دسترسی است
- [ ] قوانین فایروال پورت‌های RustDesk را مجاز می‌کنند
- [ ] (اختیاری) آیکون‌های سفارشی در `res/rawdesk/` قرار گرفته‌اند
- [ ] GitHub Actions workflow اجرا شده است
- [ ] Artifact دانلود و تست شده است

### برای کاربران نهایی

- [ ] فایل ZIP را Extract کنید
- [ ] `RAWdesk.exe` و `sciter.dll` را در محل دلخواه کپی کنید
- [ ] `RAWdesk.exe` را اجرا کنید
- [ ] هرگونه اعلان امنیتی ویندوز را بپذیرید
- [ ] اتصال به سرور را تأیید کنید
- [ ] ID خود را با تیم پشتیبانی برای دسترسی از راه دور به اشتراک بگذارید

## ساختار فایل‌ها

```
rustdesk/
├── .github/workflows/build-rawdesk.yml   # Workflow ساخت اصلی
├── res/rawdesk/                          # آیکون‌های سفارشی
├── RAWDESK_QUICKSTART.md                 # راهنمای سریع
├── RAWDESK_BUILD_GUIDE.md                # راهنمای کامل ساخت
├── RAWDESK_IMPLEMENTATION_SUMMARY.md     # خلاصه پیاده‌سازی
├── RAWDESK_PERSIAN_README.md             # این فایل
└── customize-icons.sh                    # اسکریپت جایگزینی آیکون
```

## منابع پشتیبانی

- **مستندات RustDesk**: https://rustdesk.com/docs/
- **سرور RustDesk**: https://github.com/rustdesk/rustdesk-server
- **کلاینت RustDesk**: https://github.com/rustdesk/rustdesk

## خلاصه

✅ **سفارشی‌سازی کامل RAWdesk پیاده‌سازی شد**

تمام نیازمندی‌های درخواست اولیه برآورده شده است:
- ✅ نام کلاینت: RAWdesk
- ✅ آدرس سرور: desk.timextools.ir
- ✅ کلید: vuw04gfylmMzuWj2nFEV5a2gMbfsdnKwvbwsqhGBu0o=
- ✅ رمز عبور دائمی: @dmin110
- ✅ ویندوز 32 بیتی
- ✅ دسترسی کامل
- ✅ بدون نیاز به نصب
- ✅ نوع اتصال: فقط ورودی
- ✅ پشتیبانی از آیکون و لوگوی سفارشی

**آماده برای ساخت!** → به تب Actions بروید و workflow را اجرا کنید.

---

## سؤالات متداول

**س: چگونه می‌توانم RAWdesk را بسازم؟**
ج: به تب Actions در GitHub بروید، "Build RAWdesk" را انتخاب کنید و "Run workflow" را کلیک کنید.

**س: آیا نیاز به نصب دارد؟**
ج: خیر، RAWdesk قابل حمل است و نیازی به نصب ندارد.

**س: آیا می‌توانم تنظیمات را تغییر دهم؟**
ج: تنظیمات در زمان build قفل می‌شوند و کاربران نمی‌توانند آن‌ها را تغییر دهند.

**س: چگونه آیکون سفارشی اضافه کنم؟**
ج: آیکون‌های خود را در `res/rawdesk/` قرار دهید و `./customize-icons.sh` را اجرا کنید.

**س: چرا فقط ورودی (Incoming Only) است؟**
ج: برای امنیت بیشتر و استفاده به عنوان کلاینت پشتیبانی - فقط می‌تواند اتصالات ورودی را دریافت کند.

**س: رمز عبور دائمی چیست؟**
ج: `@dmin110` - این رمز در build تعبیه شده و قابل تغییر توسط کاربران نیست.
